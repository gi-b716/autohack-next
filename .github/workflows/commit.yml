name: Process Commit

on:
  push:
    branches:
      - main
    tags:
      - "*"

defaults:
  run:
    shell: bash

jobs:
  build-normal:
    name: Build on normal commit
    uses: ./.github/workflows/build-python-package.yml
