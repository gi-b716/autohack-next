name: Process Commit

on:
  push:
    branches:
      - main
    tags:
      - "*"

defaults:
  run:
    shell: bash

jobs:
  build-normal:
    name: Build on commit
    uses: ./.github/actions/build-python-package.yml
