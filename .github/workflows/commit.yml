name: Process Commit

on:
  push:
    branches:
      - main
    tags:
      - "*"

defaults:
  run:
    shell: bash

jobs:
  build-package:
    name: Build python package on commit
    permissions:
        contents: read

    uses: ./.github/actions/build-python-package.yml
