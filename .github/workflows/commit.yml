name: Process Commit

on:
  push:
    branches:
      - main
    tags:
      - "*"

jobs:
  commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Build python package on normal commit
      uses: ./.github/workflows/build-python-package.yml
